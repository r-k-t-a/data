{
  "private": true,
  "scripts": {
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "test": "CI=true lerna run test --stream",
    "start": "lerna run --parallel start",
    "prepare": "rimraf love-*/lib && lerna run prepare",
    "build": "npm run prepare && lerna run --parallel build",
    "typecheck": "lerna run typecheck"
  },
  "devDependencies": {
    "@types/node": "^14.14.7",
    "lerna": "3.22.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.3"
  },
  "workspaces": [
    "love-store",
    "love-react",
    "dev",
    "docs"
  ]
}
